spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://db:5432/team_tracker
    username: postgres
    password: 123456789root
  #    url: ${DB_CONNECTION_IP:localhost}:${PORT:5432}/team_tracker?createDatabaseIfNotExist=true
  #    username: ${DB_CONNECTION_USERNAME:postgres}
  #    password: ${DB_CONNECTION_PASSWORD:123456789secret}
  jpa:
    #    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
    generate-ddl: true
    show_sql: true